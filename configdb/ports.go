package configdb

// CONTINUE:
// - add function to parse and validate breakout (only Ethernet[m*4](m in N) can have a breakout)
// - add function to add ports with default values depending on the breakouts
